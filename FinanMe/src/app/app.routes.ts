import { Routes } from '@angular/router';
import { ExpensesFormComponent } from './components/expenses-form/expenses-form.component';

export const routes: Routes = [
  { path: 'añadir-gasto', component: ExpensesFormComponent },
];
